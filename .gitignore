# Generated at build time by scripts/build-data.mjs - always rebuilt fresh
# before each deploy, so committing a stale copy would be actively
# misleading. See README for how to generate one locally to preview the
# site.
/public/data/matches.json

node_modules/
.env

# Written by scripts/build-snapshot.mjs / snapshot.yml's publish step when
# run locally - the real snapshot only ever lives on the `data` branch.
/snapshot/
/publish/
